#notes
```
https://github.com/HM2899/grokcli-2api
```

# Markdown 入门文档

Markdown 是一种轻量级标记语言，用纯文本编写，通过简单的符号来排版，最终可以渲染成结构化的文档。它易读易写，广泛用于写文档、笔记、README、博客等场景。

## 一、标题

用 `#` 表示标题，`#` 的数量代表层级（1 到 6 级）。

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

## 二、段落与换行

- 段落之间用一个空行分隔。
- 在行尾加两个空格再回车，可以强制换行。

```markdown
这是第一段。

这是第二段。
```

## 三、字体样式

| 语法 | 效果 |
| --- | --- |
| `*斜体*` 或 `_斜体_` | *斜体* |
| `**粗体**` 或 `__粗体__` | **粗体** |
| `***粗斜体***` | ***粗斜体*** |
| `~~删除线~~` | ~~删除线~~ |

## 四、列表

### 无序列表

用 `-`、`*` 或 `+` 开头：

```markdown
- 苹果
- 香蕉
  - 子项（缩进两个空格）
```

### 有序列表

用数字加 `.`：

```markdown
1. 第一步
2. 第二步
3. 第三步
```

### 任务列表

```markdown
- [x] 已完成
- [ ] 未完成
```

## 五、引用

用 `>` 开头，可以嵌套：

```markdown
> 这是一段引用
>> 这是嵌套引用
```

## 六、代码

### 行内代码

用反引号包裹：`` `print("hello")` ``。

### 代码块

用三个反引号包裹，可以指定语言以获得语法高亮：

````markdown
```python
def hello():
    print("Hello, Markdown!")
```
````

## 七、链接与图片

```markdown
[链接文字](https://example.com)

![图片描述](图片地址.png)
```

## 八、分割线

单独一行使用三个及以上的 `-`、`*` 或 `_`：

```markdown
---
```

## 九、表格

用 `|` 分隔单元格，第二行用 `---` 分隔表头，冒号控制对齐方式：

```markdown
| 左对齐 | 居中 | 右对齐 |
| :--- | :---: | ---: |
| a | b | c |
| 1 | 2 | 3 |
```

## 十、转义字符

如果想显示 Markdown 的特殊符号本身，在符号前加反斜杠 `\`：

```markdown
\*这不是斜体\*
```

常见需要转义的字符：`` \ ` * _ { } [ ] ( ) # + - . ! ``

## 常用速查表

| 需求 | 语法 |
| --- | --- |
| 标题 | `# 标题` |
| 粗体 | `**文字**` |
| 斜体 | `*文字*` |
| 链接 | `[文字](url)` |
| 图片 | `![描述](url)` |
| 行内代码 | `` `code` `` |
| 列表 | `- 项` |
| 引用 | `> 文字` |

## 小结

Markdown 的核心是「用最少的符号表达结构」。掌握上面这些语法，就能覆盖日常写作的绝大部分需求。建议边写边预览，很快就能形成肌肉记忆。
ali
